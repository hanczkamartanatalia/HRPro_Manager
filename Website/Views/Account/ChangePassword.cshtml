@model Website.Entities.LoginData

@{
    ViewData["Title"] = "ChangeLogin";
}


<h1 >Change your password</h1>

<div class="row main-box p-5 mt-4">
    <div class="col-md-4">
        @using (Html.BeginForm("SavePassword", "Account", FormMethod.Post))
        {
            <form asp-action="Change Password">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <label name="currentPassword" class="control-label">Current Password</label>
                    <input name="currentPassword" class="form-control mt-2" type="password" required />
                </div>
                <div class="form-group">
                    <label asp-for="Password" class="control-label">New password</label>
                    <input asp-for="Password" class="form-control mt-2" type="password" required />
                    <span asp-validation-for="Password" class="text-danger"></span>
                </div>
                <div class="form-group" style="margin-top: 20px;">
                    <input type="submit" value="Change Password" class="btn btn-outline-secondary w-100" />
                </div>
            </form>
        }
    </div>
</div>

<div style="margin-top: 30px;">
    <a asp-action="Index" class="btn btn-outline-secondary">Back to Account</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
